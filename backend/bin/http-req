#!/bin/bash

endpoint=$1
shift

if [[ "$endpoint" != /* ]]; then
    endpoint="api/$endpoint"
fi

curl "$@" --silent -H "Authorization: Bearer $T" -H "Content-type: application/json" http://localhost:4000/$endpoint | jq '.' -
